package com.swcir.swcirsystem.Repositories;

import com.swcir.swcirsystem.Models.TiposBem;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoBemRepository extends JpaRepository<TiposBem,Integer>{
    
}
